language: elixir
elixir:
  - 1.5.1
otp_releases:
  - 19.0
  - 20.0
sudo: false
env:
  install:
    - mix local.hex --force
    - mix local.rebar --force
    - mix deps.get
  script:
    - mix test
  notifications:
    - jangelod@gmail.com

